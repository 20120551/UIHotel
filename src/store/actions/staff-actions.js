import { staff } from "@constant/index";

export const getStaffInfo = (payload) => ({
  type: room.GET_ROOM_DETAIL,
  payload,
});

// export const paginationRooms = (payload) => ({
//   type: room.PAGINATION_ROOMS,
//   payload,
// });
